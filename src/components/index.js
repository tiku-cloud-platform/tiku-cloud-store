import Vue from 'vue'

import Upload from './uploadPicture/index'
Vue.component(Upload.name, Upload)

import SelectTime from './selectTime'
Vue.component(SelectTime.name, SelectTime)

import SelectTree from './TreeSelect'
Vue.component(SelectTree.name, SelectTree)
